<% provide(:title, @user.name) %>
<div class="row">

  <div class="span8">
    <% if @user.notes.any? %>
        <h3>Notes (<%= @user.notes.count %>)</h3>
        <ol class="notes">
          <%= render @notes %>
        </ol>

    <% end %>
  </div>
</div>